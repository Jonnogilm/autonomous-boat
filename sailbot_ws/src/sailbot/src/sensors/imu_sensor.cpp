//Gives stable heading at low speeds; COG alone is noisy when SOG≈0.
#include "sailbot/sensors/imu_sensor.hpp"

namespace sailbot::sensors {
// All inline / trivial for now; file exists so CMake can build it cleanly.
}  // namespace sailbot::sensors
